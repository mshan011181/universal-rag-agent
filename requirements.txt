# Core LLM & Orchestration
langchain==0.3.25
langchain-core==0.3.61
langchain-community==0.3.24
langchain-groq==0.3.2
langgraph==0.4.7

# Groq
groq==0.28.0

# Vector Store — managed Pinecone (replaces self-hosted ChromaDB)
pinecone>=3.0.0

# Embeddings (local, no API key needed)
sentence-transformers==3.4.1

# Web Search fallback (CRAG)
tavily-python==0.5.1

# Memory & DB
# sqlite3 is built-in
redis[asyncio]>=5.0.0

# Document loaders
pypdf==5.4.0
pdfplumber==0.11.6
python-docx==1.1.2
openpyxl==3.1.5
unstructured==0.17.2
python-pptx>=1.0.0      # PowerPoint .pptx/.ppt ingestion
xlrd>=2.0.0             # Legacy Excel .xls ingestion (.xlsx uses openpyxl above)

# Reranking + Hybrid Search
cohere==5.15.0
rank-bm25>=0.2.2

# Graph (GraphRAG lightweight)
networkx==3.4.2

# UI
streamlit==1.55.0

# Media ingestion (audio / video transcription)
# Groq Whisper API is built into the groq package above (whisper-large-v3)
# ffmpeg-python wraps the ffmpeg CLI for non-mp4 video audio extraction
ffmpeg-python==0.2.0

# Web page ingestion
trafilatura==2.0.0
requests>=2.32.4
beautifulsoup4>=4.12.0

# PDF table extraction (preserves row/column structure)
pdfplumber>=0.11.0

# YouTube ingestion
# youtube-transcript-api: fetches auto-generated / manual captions (caption fallback)
youtube-transcript-api>=1.2.4
# yt-dlp: downloads raw audio for Whisper transcription (primary, highest accuracy)
yt-dlp>=2026.3.17
# imageio-ffmpeg: bundles ffmpeg binary — no system install needed
imageio-ffmpeg>=0.6.0

# Utils
python-dotenv==1.1.0
pydantic==2.11.4
tiktoken==0.9.0
tenacity==9.1.2
numpy==2.2.5
pandas==2.2.3
resend>=2.0.0
httpx>=0.27.0
