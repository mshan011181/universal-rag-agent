[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --cov=src
    --cov=api
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-fail-under=0
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
