# Streamlit UI container — only what app.py needs

# Core LLM & Orchestration
langchain==0.3.25
langchain-core==0.3.61
langchain-community==0.3.24
langchain-groq==0.3.2
langgraph==0.4.7

# Groq (local dev / fallback)
groq==0.28.0

# Vertex AI Claude (production LLM — LLM_PROVIDER=vertexai)
langchain-google-vertexai==2.0.14
google-cloud-aiplatform>=1.60.0

# Vector Store — managed Pinecone (replaces self-hosted ChromaDB)
pinecone>=3.0.0

# Embeddings (local)
sentence-transformers==3.4.1

# Web Search fallback
tavily-python==0.5.1

# Document loaders
pypdf==5.4.0
pdfplumber==0.11.6
python-docx==1.1.2
openpyxl==3.1.5

# Reranking
cohere==5.15.0

# Graph
networkx==3.4.2

# UI
streamlit==1.55.0

# Media ingestion
ffmpeg-python==0.2.0
trafilatura==2.0.0
requests>=2.32.4
yt-dlp>=2026.3.17

# Utils
python-dotenv==1.1.0
pydantic==2.11.4
tenacity==9.1.2
numpy==2.2.5
pandas==2.2.3
